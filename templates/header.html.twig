<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>LIBRARY</title>
    <link rel="stylesheet" href="{{ asset('style.css') }}">
</head>
<body>
    <header>
        <a href="{{ path('accueil') }}" class="lien"><h1 class="titre">LIBRARY</h1></a>
        <div class="liens">
            <a href="{{ path('authors_list') }}" class="lien">AUTHORS</a>
            <a href="{{ path('books_list') }}" class="lien">BOOKS</a>
            <a href="{{ path('books_search') }}" class="lien">SEARCH BOOKS</a>



            <a href="{{ path('admin_authors') }}" class="lien">ADMINISTRATION</a>
        </div>
    </header>

